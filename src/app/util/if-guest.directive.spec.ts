import { IfGuestDirective } from './if-guest.directive';

describe('IfGuestDirective', () => {
  it('should create an instance', () => {
    const directive = new IfGuestDirective();
    expect(directive).toBeTruthy();
  });
});
