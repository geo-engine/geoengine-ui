import { IfGfbioLoggedInDirective } from './if-gfbio-logged-in.directive';

describe('IfGfbioLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new IfGfbioLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
