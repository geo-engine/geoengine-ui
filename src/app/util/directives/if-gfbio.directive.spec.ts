import { IfGfbioDirective } from './if-gfbio.directive';

describe('IfGfbioDirective', () => {
  it('should create an instance', () => {
    const directive = new IfGfbioDirective();
    expect(directive).toBeTruthy();
  });
});
