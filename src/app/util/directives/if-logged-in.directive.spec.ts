import { IfLoggedInDirective } from './if-logged-in.directive';

describe('IfLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new IfLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
