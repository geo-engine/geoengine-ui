/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NamePipe } from './filename.pipe';

describe('Pipe: Name', () => {
  it('create an instance', () => {
    let pipe = new NamePipe();
    expect(pipe).toBeTruthy();
  });
});
