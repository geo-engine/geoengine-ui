/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DialogPipe } from './dialog.pipe';

describe('Pipe: Dialog', () => {
  it('create an instance', () => {
    let pipe = new DialogPipe();
    expect(pipe).toBeTruthy();
  });
});
